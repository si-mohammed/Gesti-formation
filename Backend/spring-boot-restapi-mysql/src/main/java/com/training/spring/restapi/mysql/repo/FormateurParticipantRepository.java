package com.training.spring.restapi.mysql.repo;

import java.util.List;

import org.springframework.data.jpa.repository.Query;
//import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;

//import com.training.spring.restapi.mysql.model.Formateur;
//import com.training.spring.restapi.mysql.model.Session;
//import com.training.spring.restapi.mysql.model.Participant;

//public interface FormateurParticipantRepository<FormateurParticipant> extends CrudRepository <FormateurParticipant, Long>{
	/*
	 * List<Session> findByName(String name);
	 * 
	 * FormateurParticipant findById(long id);
	 */
	
	/*
	 * @Query("SELECT f.id, f.name, f.lastname, f.adress, f.email, f.tel, p.id, p.name, p.lastname, p.adress, p.email, p.tel "
	 * + "FROM Formateur f" + "FROM Participant p" ) List<FormateurParticipant>
	 * fetchAllFormateurParticipant();
	 */
	

//}
